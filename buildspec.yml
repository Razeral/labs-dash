version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 20
    commands:
      - npm ci
  build:
    commands:
      - node scripts/auto-roster.mjs --deploy
  post_build:
    commands:
      - node scripts/roster.mjs
