version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 20
    commands:
      - npm ci
      - echo "Outbound IP: $(curl -s --max-time 5 ifconfig.me)"
  build:
    commands:
      - node scripts/auto-roster.mjs --deploy
  post_build:
    commands:
      - node scripts/roster.mjs
