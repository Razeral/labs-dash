version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 20
    commands:
      - npm ci
      - bash scripts/log-ip.sh
  build:
    commands:
      - node scripts/auto-roster.mjs --deploy
  post_build:
    commands:
      - node scripts/roster.mjs
